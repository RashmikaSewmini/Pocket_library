import 'package:flutter/material.dart';

class BorrowBookPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Borrow Book")),
      body: Center(child: Text("Borrow Book Details Page")),
    );
  }
}
